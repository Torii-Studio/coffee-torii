import BakedModel from "./BakedModel";
import Smokes from "../components/Smoke";

const test = 'fail'

var outside = 'fail'

const test = (x,y) => {
  if(x != y) return x
  else return y
}

//wrong comment

const Coffee = ({}) => (
  <>
    <BakedModel />
    <Smokes />
  </>
);

export default Coffee;
